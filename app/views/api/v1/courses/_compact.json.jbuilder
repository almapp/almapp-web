json.extract! item, :id, :initials, :name, :credits, :availability, :capacity, :enrolled, :academic_unity_id, :likes_count, :dislikes_count, :comments_count
json.set! json_path_key, json_path(item) if should_show_paths?(item)